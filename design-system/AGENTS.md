# AGENTS.md — shadcn design system (v0.1.1)

> **AI entry guide.** Read this before using any component or token from
> this design-system package.

## What this is

This package is the exported design system for **shadcn**, generated by the
DS Foundations Engine. It contains typed React components and a CSS token
file (`tokens.css`).

## Golden rules

1. **Prefer these components.** Don't invent new UI primitives — pick from
   the component index below.
2. **Reference tokens as `var(--ds-*)`.** Never hard-code hex values.
   Import `tokens.css` once at your app root.
3. **Respect token families:**
   - `surface.*` → background fills
   - `content.*` / `text.*` → text / foreground colour
   - `border.*` → stroke / border colours
   - `icon.*` → glyph / SVG fill colour
4. **Don't invent styles.** Only override tokens; don't write new CSS that
   contradicts the design system.
5. **Read the component `.md` before instantiating.** Each doc lists the
   available props, when to use, and dos/don'ts.

## Importing components

```tsx
// Once in your app root:
import "@shadcn/design-system/tokens.css";

// Then anywhere:
import { Button, Input } from "@shadcn/design-system";
```

## Component index (public)

| Component | Description | Doc | Source |
|-----------|-------------|-----|--------|
| `Badge` | A small, saturated indicator that emphasises a count, status or label. | [Badge.md](components/Badge.md) | [Badge.tsx](components/Badge.tsx) |
| `ButtonGroup` | A horizontally or vertically aligned set of related Buttons, typically the commi | [ButtonGroup.md](components/ButtonGroup.md) | [ButtonGroup.tsx](components/ButtonGroup.tsx) |
| `Card` | A self-contained surface that groups a heading, body, optional media and actions | [Card.md](components/Card.md) | [Card.tsx](components/Card.tsx) |
| `Checkbox` | A toggle that selects or deselects an independent option; multiple checkboxes ca | [Checkbox.md](components/Checkbox.md) | [Checkbox.tsx](components/Checkbox.tsx) |
| `FieldGroup` | shadcn/ui FieldGroup. A gap-7 column of composed Fields (real instances of your  | [FieldGroup.md](components/FieldGroup.md) | [FieldGroup.tsx](components/FieldGroup.tsx) |
| `InputGroup` | shadcn/ui InputGroup. A single bordered row composing a borderless input with le | [InputGroup.md](components/InputGroup.md) | [InputGroup.tsx](components/InputGroup.tsx) |
| `Label` | shadcn/ui Label. A single inline form label (text-sm medium) bound to shadcn sem | [Label.md](components/Label.md) | [Label.tsx](components/Label.tsx) |
| `RadioGroup` | A labelled set of Radios where exactly one option must be selected. | [RadioGroup.md](components/RadioGroup.md) | [RadioGroup.tsx](components/RadioGroup.tsx) |
| `Select` | A control that opens a list of options and displays the chosen one in a trigger  | [Select.md](components/Select.md) | [Select.tsx](components/Select.tsx) |
| `Switch` | A binary control that turns a setting on or off and applies the change immediate | [Switch.md](components/Switch.md) | [Switch.tsx](components/Switch.tsx) |
| `Textarea` | shadcn/ui Textarea. Multi-line text field across sizes (sm, md, lg) — rounded-md | [Textarea.md](components/Textarea.md) | [Textarea.tsx](components/Textarea.tsx) |
| `ToggleGroup` | shadcn/ui ToggleGroup. A row of toggle items sharing the Toggle styling — joined | [ToggleGroup.md](components/ToggleGroup.md) | [ToggleGroup.tsx](components/ToggleGroup.tsx) |

## Tokens

All `--ds-*` CSS custom properties are documented in [tokens.md](tokens.md).
The raw DTCG tree is in `tokens.json` for build-time tooling.

## Do NOT use directly

The following are **internal sub-components** — implementation details of
the public components above. Import the parent component instead.

- `Button`
- `Field`
- `Input`
- `Input`
