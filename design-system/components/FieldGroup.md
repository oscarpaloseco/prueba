# FieldGroup

shadcn/ui FieldGroup. A gap-7 column of composed Fields (real instances of your saved components, grouped by the body composer) with an optional 'Or' FieldSeparator between groups. Fully bound to shadcn semantic tokens.

## Usage example

```tsx
<FieldGroup />
```
